NAME=trilinos-9.0.3
SOURCE=http://trilinos.sandia.gov/download/files/
PACKING=.tar.gz
BUILDCHAIN=autotools
TARGETS=('' install clean)
CONFOPTS="--enable-pytrilinos --enable-amesos --cache-file=config.cache --with-cflags=-fPIC --with-cxxflags=-fPIC --with-fflags=-fPIC"

package_specific_setup () {
    export TRILINOS_DIR=${INSTALL_PATH}
    patch packages/PyTrilinos/src/FILEstream.cpp < ${ORIGDIR}/patches/trilinos.patch
}
